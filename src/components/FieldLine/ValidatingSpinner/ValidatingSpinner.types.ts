/**
 * Props for the ValidatingSpinner component
 */
export interface IValidatingSpinnerProps {
  /**
   * True if the Field is currently validating
   * and thus the spinner should be visible
   */
  isValidating: boolean;
}
